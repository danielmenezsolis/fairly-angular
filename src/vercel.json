{
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/fairly-app/browser",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}